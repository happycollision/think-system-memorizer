<SwiperContainer class="h-screen" @currentSlide={{currentIndex}} @keyboard={{true}} as |container|>
  {{#each @items key="id" as |item|}}
    <container.slide>
      {{yield item}}
    </container.slide>
  {{/each}}
</SwiperContainer>